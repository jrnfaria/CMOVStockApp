<Page x:ConnectionId='1'
    x:Class="CMOVStockApp.Views.Shares"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CMOVStockApp.Views"
    xmlns:models="using:CMOVStockApp.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
        <RelativePanel HorizontalAlignment="Center" >
            <ListView x:ConnectionId='9' ItemsSource="{Binding observingList}"
          VerticalAlignment="Center" HorizontalAlignment="Stretch" x:Name="observingCompanyList" Margin="-3,0,0,0" >
                <ListView.Header>
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" FontWeight="Bold"  Text="Symbol" />
                        <TextBlock Grid.Column="1" FontWeight="Bold" Text="Name" />
                    </Grid>
                </ListView.Header>

                <ListView.ItemTemplate>
                    <DataTemplate                            >
                        <StackPanel x:ConnectionId='11' HorizontalAlignment="Stretch">
                            <TextBlock x:ConnectionId='12' Grid.Column="0"                        FontWeight="Bold" />
                            <TextBlock x:ConnectionId='13' Grid.Column="1"                      FontWeight="Bold"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button x:ConnectionId='10' x:Name="AddStock" Content="Add stock" RelativePanel.AlignHorizontalCenterWith="observingCompanyList" RelativePanel.Below="observingCompanyList" Height="Auto" MinWidth="100"  MaxWidth="1000"  VerticalAlignment="Top"                   />

        </RelativePanel>

        <ContentDialog x:ConnectionId='2' x:Name="contentDialog"
                        VerticalAlignment="Stretch"
                        Title="Company search"
                        PrimaryButtonText="Add"
                        IsPrimaryButtonEnabled="{Binding IsChecked, Mode=OneWay}"
                        SecondaryButtonText="Cancel"
                        MaxWidth="{Binding ActualWidth, ElementName=pageRoot}"                                >
            <StackPanel>
                <TextBlock x:ConnectionId='3' Text="Add to watching list"
                            TextWrapping="Wrap"  x:Name="searchText"/>
                <TextBox x:ConnectionId='4' x:Name="SearchBar"                              />
                <ListView x:ConnectionId='5'                                    
        HorizontalAlignment="Center" VerticalAlignment="Center"  x:Name="searchList">
                    <ListView.ItemTemplate>
                        <DataTemplate                            >
                            <StackPanel x:ConnectionId='6' Orientation="Horizontal">
                                <TextBlock x:ConnectionId='7'                        HorizontalAlignment="Left" FontWeight="Bold"/>
                                <TextBlock x:ConnectionId='8'                      HorizontalAlignment="Right" Margin="10,0,0,0" FontWeight="Bold"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>


